apiVersion: v1
kind: Service
metadata:
  name: startup-pass
  labels:
    mode: startup
spec:
  ports:
  - name: rest
    nodePort: 30300
    port: 3000
    protocol: TCP
  selector:
    app: startup-pass
  type: NodePort
