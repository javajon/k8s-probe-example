apiVersion: v1
kind: Service
metadata:
  name: probe-example-startup
spec:
  ports:
  - name: rest
    nodePort: 30300
    port: 3000
    protocol: TCP
  selector:
    app: probe-example-startup
  type: NodePort
